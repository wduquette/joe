package com.wjduquette.joe.nero;

/**
 * Aggregator function parameter types.
 */
public enum AggParm {
    /** A bound variable to aggregate over. */
    VAR,

    /**
     * A constant or bound variable whose value is used as data by
     * the aggregation function.
     */
    VAL
}
