// lang_ruleset.joe
//
// The purpose of this test suite is simply to verify that a Nero ruleset can be
// created in a Joe script. Nero semantics is tested by `RuleEngineTest`,
// Nero parsing is tested by `NeroParserTest`, and the behavior of an
// embedded Nero ruleset is tested in `type.joe.Ruleset.joe`.

function testValid() {
    var rules = ruleset {
        define Parent/p,c;
        define Ancestor/a,d;
        Parent(#walker, #bert);
        Parent(#bert, #clark);
        Ancestor(x, y) :- Parent(x, y);
        Ancestor(x, y) :- Parent(x, z), Ancestor(z, y);
    };
}


